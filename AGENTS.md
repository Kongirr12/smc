# AGENTS.md — MHC Smart School Project Memory & Guidelines

เอกสารข้อกำหนดและบันทึกความจำของโปรเจกต์ **MHC Smart School** สำหรับ AI Agent (Antigravity) และผู้พัฒนาระบบ

---

## 📌 ข้อมูลอัตลักษณ์ประจำโปรเจกต์ (Project Identity)

- **ชื่อระบบ:** MHC Smart School (ระบบบริหารจัดการโรงเรียนอัจฉริยะ)
- **เวอร์ชันพัฒนาล่าสุด (Latest Production Version):** **`v2.7.8`** (24 กันยายน 2569)
- **สถานศึกษา:** **โรงเรียนมหาชัยพิทยาคาร** ตำบลท่าสองคอน อำเภอเมือง จังหวัดมหาสารคาม ๔๔๐๐๐
- **ผู้อำนวยการโรงเรียน:** **นายอธิการ สุขศรี**
- **ผู้จัดทำ / ผู้พัฒนาระบบ:** **ครูก้องนที อุ่นเจริญ**
- **GitHub Repository:** `https://github.com/Kongirr12/smc.git`
- **PWA Service Worker Cache Name:** `mhc-smart-school-v2.7.8`

---

## 🔒 กฎสำคัญที่ต้องปฏิบัติตามเสมอ (Core Inviolable Rules)

1. **เวอร์ชันล่าสุดคือ v2.7.8:**
   - ห้ามดาวน์เกรดหรือถอยกลับเวอร์ชันเก่า ทุกการปรับปรุงในอนาคตจะต้องต่อยอดจากเวอร์ชัน **`v2.7.8`** เท่านั้น (เช่น `v2.7.9` หรือ `v2.8.0`)
   - ทุกครั้งที่มีการแก้ไข Frontend ต้องอัปเดต Query String `?v=...` ใน `index.html` และ `CACHE_NAME` ใน `sw.js` เพื่อเคลียร์แคช

2. **ชื่อผู้อำนวยการโรงเรียน:**
   - ต้องใช้ **"นายอธิการ สุขศรี"** ในทุกเอกสารราชการ, แบบบันทึกผลการเรียน ปพ.5, ตารางสอนประจำชั้น, ตารางสอนรายครู, ปฏิทินกิจกรรม และการตั้งค่าระบบ (`director_name`)
   - ห้ามใส่ชื่อตัวอย่างอื่นเด็ดขาด

3. **ชื่อโรงเรียนและสังกัด:**
   - ใช้ **"โรงเรียนมหาชัยพิทยาคาร"** สังกัดสำนักงานเขตพื้นที่การศึกษามัธยมศึกษามหาสารคาม (สพม.มหาสารคาม)
   - ที่อยู่ในบันทึกข้อความภายใน: `ส่วนราชการ  โรงเรียนมหาชัยพิทยาคาร ตำบลท่างสองคอน อำเภอเมือง จ.มหาสารคาม ๔๔๐๐๐` (ไม่มี "ศธ" นำหน้า สำหรับใช้ภายในหน่วยงาน)

4. **ชื่อผู้จัดทำ / ผู้พัฒนา:**
   - ต้องระบุ **"ครูก้องนที อุ่นเจริญ"** ในแถบ Footer, หน้าเข้าสู่ระบบ, หน้าคู่มือการใช้งาน และหัวเอกสารทั้งหมด

5. **ระบบแบบฟอร์มเอกสาร 4 ฝ่าย:**
   - มีแบบฟอร์มมาตรฐาน 30 แบบฟอร์มครอบคลุม:
     - ฝ่ายบริหารวิชาการ (8 แบบฟอร์ม)
     - ฝ่ายบริหารงบประมาณและแผนงาน (8 แบบฟอร์ม)
     - ฝ่ายบริหารงานบุคคล (7 แบบฟอร์ม)
     - ฝ่ายบริหารทั่วไป (7 แบบฟอร์ม)
   - ฟังก์ชัน CRUD (เพิ่ม / ลบ / แก้ไข / คืนค่าเริ่มต้น / ดาวน์โหลด Word / พิมพ์ A4) ต้องใช้งานได้อย่างสมบูรณ์

---

## ⚡ สถาปัตยกรรมและเทคโนโลยี (Architecture)

- **Frontend:** HTML5, Modern CSS (Tailwind + Custom Aurora Theme), Vanilla JavaScript (Modular: `js1.js` ถึง `js4.js`, `js_templates.js` ฯลฯ)
- **Backend:** Google Apps Script (`code.gs`)
- **Database:** Google Sheets (ชีต `Config`, `Users`, `Sessions`, `Students`, `Personnel`, `Attendance`, `Academic`, `Schedule`, `Classrooms`, `Behavior` ฯลฯ)
- **Caching & Performance:**
  - Micro-cache ใน Client (`CACHE_TTL_MAP`)
  - Server Cache ใน `CacheService.getScriptCache()`
  - Direct Cell Update `(idx + 2)` สำหรับการอัปเดตข้อมูลความเร็วสูง
  - `LockService.getScriptLock()` รอบการบันทึกข้อมูลสำคัญ
